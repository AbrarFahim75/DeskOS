This folder holds runtime data DeskOS creates on your machine
(habit/history database, remembered widget position). It is empty in the
repository on purpose — it fills in as you use the app — and is excluded
from version control via .gitignore.
