# Assets

Images used by the project README and documentation.

Drop the banner here as `banner.png` (recommended 1600x900 or wider), then
uncomment the banner line at the top of the root `README.md`.
